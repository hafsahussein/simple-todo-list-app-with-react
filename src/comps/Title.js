import React, { Component } from 'react'

export default class Title extends Component {
    render() {
        return (
            <h1 className="title">Simple to do list app</h1>
        )
    }
}
